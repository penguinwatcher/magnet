#!/usr/bin/env python
# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

if __name__ == '__main__':
    setup(
        name = 'magnet',
        packages = find_packages(),
        entry_points = """
        [console_scripts]
        """
    )

